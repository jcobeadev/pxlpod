import { TabPlaceholder } from "../../src/components/TabPlaceholder";

// TODO: replace with the real Home screen (design screen "04 Home") in the
// next batch — this shell batch only builds the tab bar it lives inside.
export default function HomeTab() {
  return <TabPlaceholder label="Home" />;
}
