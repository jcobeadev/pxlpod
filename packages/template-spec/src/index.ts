/**
 * @poplab/template-spec
 *
 * The template format and the maths that lay it out. Imported by the mobile app
 * (Skia), the web booth (Canvas 2D) and the operator console (Konva) — three
 * renderers, one definition of where a slot sits.
 */

export * from "./schema.ts";
export * from "./layout.ts";
